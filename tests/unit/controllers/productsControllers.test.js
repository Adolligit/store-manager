const { expect } = require("chai");
const sinon = require("sinon");

const ProductController = require("../../../controllers/Product");
const ProductService = require("../../../services/Product");

describe("[CONTROLLER: Product] Verificando requisições HTTP", () => {

  // const response = {};
  // const request = {};

  // before(() => {
  //   request.body = {};

  //   response.status = sinon.stub().returns(response);
  //   response.json = sinon.stub();

  //   sinon.stub(ProductService, "all").response([]);
  // });

  // it("[GET: '/products'] Todos os produtos serão retornados ", async () => {
  //   const product = await ProductController.all(request, response);
   
  //   expect(product).to.be.eq([]);
  // });
});
